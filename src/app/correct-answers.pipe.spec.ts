import { CorrectAnswersPipe } from './correct-answers.pipe';

describe('CorrectAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new CorrectAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
